git checkout --orphan latest_branch

git add -A

git commit -m "Initial commit".
​
git branch -D main

git branch -m main

git push -f origin main

cd Cup-Of-Liber-Tea; git checkout --orphan latest_branch; git add -A; git commit -m "Initial commit"; git branch -D main; git branch -m main; git push -f origin main